# Example plugin metadata (phase 1: metadata only)
# Runtime registration hooks will be wired in a later step.

PLUGIN = {
    "name": "coverage_drilldown",
    "version": "0.1.0",
    "description": "Per-set coverage drilldown and missing-only reporting (scaffolding).",
    "requires_api": 1,
}
