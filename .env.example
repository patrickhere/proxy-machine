# Proxy Machine Environment Configuration
# Copy this file to .env and customize for your deployment

# ==============================================================================
# PATH CONFIGURATION
# ==============================================================================

# Project root directory (where proxy-machine code lives)
# Docker/Unraid: This should be /app if using standard Docker setup
#PROXY_MACHINE_ROOT=/app

# Shared asset library (tokens, lands, card-backs)
# Docker/Unraid: Map this to a persistent volume
#SHARED_ROOT=/data/shared

# Profiles root directory (user decks and PDFs)
# Docker/Unraid: Map this to a persistent volume
#PROFILES_ROOT=/data/profiles

# Bulk data directory (Scryfall database files)
# Docker/Unraid: Map this to a persistent volume (3GB+ required)
#BULK_DATA_DIR=/data/bulk-data

# Database file path
# Docker/Unraid: Will be created in BULK_DATA_DIR if not specified
#DATABASE_PATH=/data/bulk-data/bulk-index.db

# PDF output directory override (optional)
# Leave blank to use profile-specific paths
#PDF_OUTPUT_DIR=

# Reports directory
#REPORTS_DIR=/data/shared/reports

# ==============================================================================
# WEB SERVER CONFIGURATION
# ==============================================================================

# Web server host (use 0.0.0.0 for Docker/Unraid to allow external access)
WEB_HOST=0.0.0.0

# Web server port
WEB_PORT=5001

# ==============================================================================
# TAILSCALE CONFIGURATION (Future Feature)
# ==============================================================================

# Enable Tailscale integration
#TAILSCALE_ENABLED=true

# Tailscale hostname for accessing the service
#TAILSCALE_HOSTNAME=proxy-machine

# ==============================================================================
# DOCKER/UNRAID EXAMPLE VOLUME MAPPINGS
# ==============================================================================
# When running in Docker/Unraid, map these paths:
#
# -v /mnt/user/proxy-machine/shared:/data/shared
# -v /mnt/user/proxy-machine/profiles:/data/profiles
# -v /mnt/user/proxy-machine/bulk-data:/data/bulk-data
# -p 5001:5001
#
# Then set the environment variables above to use /data/* paths
